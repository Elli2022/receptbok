# Required for accounts, public recipes, and saved recipes.
# Use the same MongoDB Atlas database as the old backend if you want existing
# recipes in the old `recipes` collection to appear in the public library.
DATABASE_URL=mongodb+srv://...

# Required in production so login cookies can be signed safely.
AUTH_SECRET=replace-with-a-long-random-string
